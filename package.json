{
  "name": "md2confluence-mcp",
  "version": "0.1.10",
  "description": "MCP server for uploading Markdown to Confluence with images",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "md2confluence-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "npm run build && cd dist && node --test",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "confluence",
    "markdown",
    "atlassian",
    "claude",
    "mermaid"
  ],
  "author": "Daegyeom Kim",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Gyeom/md2confluence-mcp.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "marked": "^12.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
